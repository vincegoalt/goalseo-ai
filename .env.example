# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Configuration (PostgreSQL - Neon, Supabase, or local)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Polar.sh Payment Configuration
# Get these from: https://polar.sh
POLAR_ACCESS_TOKEN=your_polar_access_token
POLAR_SERVER=sandbox # Use "production" for live environment
POLAR_WEBHOOK_SECRET=your_polar_webhook_secret
POLAR_SUCCESS_URL=success # Redirect path after successful payment

# Polar Product Configuration
# Create products in your Polar dashboard
NEXT_PUBLIC_STARTER_TIER=your_starter_product_id
NEXT_PUBLIC_STARTER_SLUG=starter

# Better Auth Configuration
BETTER_AUTH_SECRET=your_random_secret_key_here # Generate with: openssl rand -base64 32