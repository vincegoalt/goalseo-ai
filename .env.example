# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Configuration (PostgreSQL - Neon, Supabase, or local)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Polar.sh Payment Configuration
# Get these from: https://polar.sh
POLAR_ACCESS_TOKEN=your_polar_access_token
POLAR_SERVER=sandbox # Use "production" for live environment
POLAR_WEBHOOK_SECRET=your_polar_webhook_secret
POLAR_SUCCESS_URL=success # Redirect path after successful payment

# Polar Product Configuration
# Create products in your Polar dashboard
NEXT_PUBLIC_STARTER_TIER=your_starter_product_id
NEXT_PUBLIC_STARTER_SLUG=starter

# Better Auth Configuration
BETTER_AUTH_SECRET=your_random_secret_key_here # Generate with: openssl rand -base64 32

# Free LLM API Keys for SEO Audit Tool
# The audit tool will try these in order and fallback to the next if one fails

# Option 1: Groq (RECOMMENDED - Fast and reliable)
# Get your free API key at: https://console.groq.com/keys
# Free tier: 30,000 tokens/minute, 14,400 requests/day
GROQ_API_KEY=

# Option 2: HuggingFace (Good fallback)
# Get your free API key at: https://huggingface.co/settings/tokens
# Free tier: Rate limited but generous for testing
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Option 3: Cohere (Additional fallback)
# Get your free API key at: https://dashboard.cohere.com/api-keys
# Free tier: 1000 API calls/month
COHERE_API_KEY=your_cohere_api_key_here

# Note: You only need ONE of these API keys for the audit tool to work.
# Having multiple keys provides redundancy if one service is down.
# If no API keys are configured, the tool will use a basic analysis algorithm.